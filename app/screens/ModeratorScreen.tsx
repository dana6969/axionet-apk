export default function ModeratorScreen() { const [flagged, setFlagged] = require('react').useState(false); const [content, setContent] = require('react').useState(''); const check = () => setFlagged(/(banned|violation|exploit)/i.test(content)); return require('react').createElement(require('react-native').View, { className: 'p-4' }, require('react').createElement(require('react-native').Text, { className: 'text-xl font-bold mb-2' }, 'Axionet Moderator'), require('react').createElement(require('react-native').TextInput, { className: 'border p-2 mb-2 rounded', placeholder: 'Enter content to scan...', value: content, onChangeText: setContent }), require('react').createElement(require('react-native').Button, { title: 'Moderate', onPress: check }), require('react').createElement(require('react-native').Text, { className: 'mt-4' }, flagged ? '⚠️ Content flagged' : '✅ Content is clean')); }
