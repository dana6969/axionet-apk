export default function MeshCommScreen() { const [msg, setMsg] = require('react').useState(''); const [sent, setSent] = require('react').useState(false); const send = () => { setSent(true); setTimeout(() => setSent(false), 1500); }; return require('react').createElement(require('react-native').View, { className: 'p-4' }, require('react').createElement(require('react-native').Text, { className: 'text-xl font-bold mb-2' }, 'Axionet Mesh Node'), require('react').createElement(require('react-native').TextInput, { className: 'border p-2 mb-2 rounded', placeholder: 'Enter message...', value: msg, onChangeText: setMsg }), require('react').createElement(require('react-native').Button, { title: 'Send over mesh', onPress: send }), require('react').createElement(require('react-native').Text, { className: 'mt-4' }, sent ? `📡 Sent: ${msg}` : '')); }
