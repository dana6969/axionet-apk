import React from 'react'; import { View, Text, Button, StyleSheet } from 'react-native'; export default function MeshCommScreen() { return (<View style={styles.container}><Text style={styles.text}>Mesh Comm Mode</Text><Button title='Mesh Ping' onPress={() => console.log('Run mesh comm logic here')} /></View>); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#000' }, text: { color: '#0ff', fontSize: 22, marginBottom: 12 } });
