import React from 'react'; import { View, Button, Text, StyleSheet } from 'react-native'; export default function SystemFilesScreen() { return (<View style={styles.container}><Text style={styles.text}>SystemFiles Mode</Text><Button title='SystemFiles Action' onPress={() => alert('SystemFiles triggered')} /></View>); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#000' }, text: { color: '#0ff', fontSize: 22, marginBottom: 12 } });
