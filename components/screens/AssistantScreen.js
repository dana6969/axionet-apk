import React from 'react'; import { View, Button, Text, StyleSheet } from 'react-native'; import { speak } from '../lib/voice'; export default function AssistantScreen() { return (<View style={styles.container}><Text style={styles.text}>Assistant Mode</Text><Button title='Speak Status' onPress={() => speak("Assistant is online")} /><Button title='Assistant Action' onPress={() => alert('Assistant triggered')} /></View>); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#000' }, text: { color: '#0ff', fontSize: 22, marginBottom: 12 } });
