import React from 'react'; import { View, Button, Text, StyleSheet } from 'react-native'; import { authenticate } from '../lib/biometric'; export default function PersonalAssistantScreen() { return (<View style={styles.container}><Text style={styles.text}>PersonalAssistant Mode</Text><Button title='Biometric Login' onPress={authenticate} /><Button title='PersonalAssistant Action' onPress={() => alert('PersonalAssistant triggered')} /></View>); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#000' }, text: { color: '#0ff', fontSize: 22, marginBottom: 12 } });
