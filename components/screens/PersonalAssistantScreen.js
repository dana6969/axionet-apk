import React from 'react'; import { View, Text, Button, StyleSheet } from 'react-native'; import { speak } from '../../lib/voice'; import { authenticate } from '../../lib/biometric'; export default function PersonalAssistantScreen() { return (<View style={styles.container}><Text style={styles.text}>Personal Assistant</Text><Button title='Check Status' onPress={() => speak('Personal Assistant is active')} /><Button title='Biometric Unlock' onPress={authenticate} /></View>); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#000' }, text: { color: '#0ff', fontSize: 22, marginBottom: 12 } });
