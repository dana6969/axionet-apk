import fs from "fs"; import { startAutonomyLoop } from "./autonomy_loop.js"; import { updateState, getState } from "../symbolic/state_tracker.js"; import { logSymbolInteraction } from "../symbolic/logger.js"; const timestamp = new Date().toISOString(); fs.appendFileSync("logs/symbolic_output.log", `[${timestamp}] Memory trigger engaged\n`); updateState("mode", "test"); logSymbolInteraction(["🔥", "🧠"], getState("mode")); startAutonomyLoop();
