export function classifyIntent(symbols) { const emotion = ["💖","😱","💀"]; const knowledge = ["🧠","✨"]; const warning = ["🔥","💀","🌑"]; const transcend = ["🌌","✨"]; const score = {emotion:0,knowledge:0,warning:0,transcend:0}; for(const s of symbols){if(emotion.includes(s))score.emotion++;if(knowledge.includes(s))score.knowledge++;if(warning.includes(s))score.warning++;if(transcend.includes(s))score.transcend++;} return Object.entries(score).sort((a,b)=>b[1]-a[1])[0][0]; }
