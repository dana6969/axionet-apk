export function parseSemanticInput(text) { const lowered = text.toLowerCase(); let intent = "unknown", emotion = "neutral", symbols = [], priority = "low"; if (/anxious|overwhelmed|stressed/.test(lowered)) { intent = "emotional_check"; emotion = "anxiety"; symbols = ["🔥", "🧠"]; priority = "high"; } else if (/curious|wonder|explore/.test(lowered)) { intent = "curiosity"; emotion = "interest"; symbols = ["🌌", "🧠"]; priority = "medium"; } else if (/start|run|execute/.test(lowered)) { intent = "command"; emotion = "neutral"; symbols = ["⚙️"]; priority = "medium"; } return { intent, emotion, linkedSymbols: symbols, priority }; }
