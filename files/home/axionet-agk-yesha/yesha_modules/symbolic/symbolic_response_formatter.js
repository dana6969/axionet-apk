export function formatSymbolicResponse({ symbols, reasoning, expansion, context }) { const time = new Date().toISOString(); return `[${time}] ➤ Symbols: ${symbols.join(", ")} | Action: ${reasoning.action}${reasoning.level ? " (" + reasoning.level + ")" : ""}${reasoning.depth ? " [depth: " + reasoning.depth + "]" : ""}${expansion?.prompt ? " → " + expansion.prompt : ""} | Context: ${context}`; }
