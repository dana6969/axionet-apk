import coreReasoner from "./core_reasoner.js"; import { addToChain } from "./symbol_chain_handler.js"; export function logSymbolInteraction(symbols, context) { const result = coreReasoner(symbols, { context }); const timestamp = new Date().toISOString(); addToChain(symbols, result, context); console.log(`[${timestamp}] Yehsa AGI Log | Symbols: ${symbols.join(", ")} | Context: ${context} | Reasoning: ${JSON.stringify(result)}`); }
