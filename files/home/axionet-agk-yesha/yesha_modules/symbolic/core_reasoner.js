export default function coreReasoner(symbols, state) { if (!symbols || symbols.length === 0) return { action: "idle" }; if (symbols.includes("🔥")) return { action: "alert", level: "high" }; if (symbols.includes("🧠")) return { action: "reflect", depth: "deep" }; if (symbols.includes("🌌")) return { action: "expand", topic: "cosmic" }; return { action: "process", symbols }; }
