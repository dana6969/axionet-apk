module.exports = function coreReasoner(symbols, state = {}) { if (!Array.isArray(symbols) || symbols.length === 0) return { action: "idle" }; const priority = { "🔥": "alert", "🧠": "reflect", "🌌": "expand", "⚙️": "compute", "🔍": "analyze", "💡": "insight", "⛓️": "chain", "🔁": "loop" }; for (const sym of Object.keys(priority)) { if (symbols.includes(sym)) return { action: priority[sym], context: state.context || "default" }; } return { action: "process", symbols, context: state.context || "general" }; };
