import { parseTextToSymbols } from "./yesha_modules/core/text_parser.js"; import { classifyIntent } from "./yesha_modules/core/intent_classifier.js"; import { executeAction } from "./yesha_modules/core/action_executor.js"; import { logSymbolInteraction } from "./yesha_modules/symbolic/logger.js"; const inputText = "fear of intelligence beyond light"; const symbols = parseTextToSymbols(inputText); const intent = classifyIntent(symbols); logSymbolInteraction(symbols, "phase4:input_text:" + inputText); executeAction({ action: intent.intent === "emotion" ? "alert" : "process", level: "high", context: "phase4:" + intent.intent });
