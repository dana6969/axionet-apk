export function logToLearningQueue(entry) { import("fs").then(fs => { const path = "./symbolic_memory/learning_queue.json"; fs.readFile(path, "utf8").then(data => { let queue = []; try { queue = JSON.parse(data); } catch {} queue.push(entry); fs.writeFile(path, JSON.stringify(queue, null, 2)); }).catch(() => { fs.writeFile(path, JSON.stringify([entry], null, 2)); }); }); }
