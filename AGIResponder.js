const fs = require('fs'); const triggers = [/shutdown/i, /LORA_FAKE/i, /BLE/i]; const memory = JSON.parse(fs.readFileSync('mesh.memory.json', 'utf8')); console.log('[AGIResponder] 🧠 Scanning', memory.length, 'entries...'); memory.forEach(entry => { triggers.forEach(t => { if (t.test(JSON.stringify(entry))) { console.log('[AGIResponder] ⚡ Trigger matched:', t, '=>', entry); } }); });
