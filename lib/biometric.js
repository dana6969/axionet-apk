import * as Auth from "expo-local-authentication"; export const authenticate = async () => { const available = await Auth.hasHardwareAsync(); if (!available) return { success: false }; return await Auth.authenticateAsync(); };
